version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "db"
        out: "./gen"
        overrides:
          - go_type: "github.com/google/uuid.UUID"
            db_type: "pg_catalog.uuid"
